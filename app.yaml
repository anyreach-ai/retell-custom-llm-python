service: llm-backend
runtime: python311

handlers:
  - url: /static
    static_dir: static

  - url: /.*
    script: auto

entrypoint: uvicorn src.server:app --host="0.0.0.0" --port=${PORT:-8080}

env_variables:
  GOOGLE_CLOUD_PROJECT: "retell-backend"
  


